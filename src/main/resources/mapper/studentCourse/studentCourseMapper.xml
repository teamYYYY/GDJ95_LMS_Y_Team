<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mapper.studCourse.StudCourseMapper">
	<!-- 수강 신청 가능 강의 리스트 -->
	<select id="selectCourseListForStudent" resultType="com.example.lms.dto.CourseDTO">
	
	
	</select>	
	<!-- 강의 정원 조회 -->
	<select id="getCourseCapacity" parameterType="int" resultType="int">
		SELECT c.course_capacity
		FROM tb_course c
		WHERE c.course_no=#{courseNo}
	</select>
	
	<!-- 현재 수강신청 인원 -->
	<select id="getCurrentEnrollment" parameterType="int" resultType="int">
		SELECT COUNT(*)
		FROM enrollment e
		WHERE e.course_co=#{courseNo}
		AND e.enrollment_status=0
	</select>
	
</mapper>