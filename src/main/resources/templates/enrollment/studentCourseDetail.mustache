<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>{{detail.courseName}} | 강의 상세 정보</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/pretendard/dist/web/static/pretendard.css" />
	<link rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
	
    <style>
        body { font-family: 'Pretendard', sans-serif; }
    </style>
</head>

<body class="bg-[#F5F7FA] text-gray-800">

{{> studentLayout/studentHeader }}

<main class="max-w-5xl mx-auto px-4 py-10">

    <div class="bg-white rounded-xl shadow p-8 border border-gray-100">

        <!-- 제목 -->
        <h2 class="text-2xl font-semibold text-gray-900 mb-8">
            {{detail.courseName}}
        </h2>

        <!-- 기본 정보 -->
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6 text-sm leading-6">

            <div class="space-y-2">
                <p><span class="font-medium text-gray-700">교수명:</span> {{detail.professorName}}</p>
                <p><span class="font-medium text-gray-700">학과:</span> {{detail.deptName}}</p>
                <p><span class="font-medium text-gray-700">학점:</span> {{detail.courseScore}} 학점</p>
                <p><span class="font-medium text-gray-700">정원:</span> {{detail.courseCapacity}} 명</p>
            </div>

            <div class="space-y-2">
                <p><span class="font-medium text-gray-700">개설년도:</span> {{detail.courseYear}}</p>
                <p><span class="font-medium text-gray-700">학기:</span> {{detail.courseSemester}} 학기</p>
                <p><span class="font-medium text-gray-700">강의실:</span> {{detail.courseClassroom}}</p>
            </div>

        </div>

        <!-- 구분선 -->
        <hr class="my-8">

        <!-- 강의 설명 -->
        <div class="mb-10">
            <h3 class="text-lg font-medium text-gray-900 mb-3">강의 설명</h3>
            <div class="bg-gray-50 rounded-lg p-4 border text-sm text-gray-700 whitespace-pre-wrap">
                {{detail.courseDescription}}
            </div>
        </div>

        <!-- 강의 시간표 -->
        <div>
            <h3 class="text-lg font-medium text-gray-900 mb-3">강의 시간표</h3>

            <table class="w-full text-sm border-collapse border border-gray-200 rounded-lg overflow-hidden">
                <thead class="bg-gray-50 border-b">
                    <tr>
                        <th class="py-2 px-3 text-left">요일</th>
                        <th class="py-2 px-3 text-left">교시</th>
                    </tr>
                </thead>

                <tbody>
                    {{#detail.courseTimeList}}
                    <tr class="border-b">
                        <td class="py-2 px-3">{{yoilName}}</td>
                        <td class="py-2 px-3">{{courseTimeStart}} ~ {{courseTimeEnd}} 교시</td>
                    </tr>
                    {{/detail.courseTimeList}}

                    {{^detail.courseTimeList}}
                    <tr>
                        <td colspan="2" class="py-4 text-center text-gray-500">
                            등록된 강의 시간이 없습니다.
                        </td>
                    </tr>
                    {{/detail.courseTimeList}}
                </tbody>
            </table>
        </div>

        <!-- 뒤로가기 -->
		<div class="mt-10 flex justify-end">
		    <button onclick="history.back()"
		        class="inline-flex items-center gap-2 px-4 py-2 rounded-xl border border-gray-200
		               bg-white text-gray-700 shadow-sm hover:bg-gray-50 transition text-sm">
		        <i class="fa-solid fa-arrow-left text-gray-500"></i>
		        목록으로
		    </button>
		</div>


    </div>

</main>

{{> studentLayout/studentFooter }}

</body>
</html>
