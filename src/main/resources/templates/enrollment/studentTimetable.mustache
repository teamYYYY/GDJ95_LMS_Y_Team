<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>내 시간표</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/pretendard/dist/web/static/pretendard.css" />

    <style> body { font-family: 'Pretendard', sans-serif; } </style>
</head>

<body class="bg-[#F5F7FA] text-gray-800">

{{> studentLayout/studentHeader }}

<main class="max-w-6xl mx-auto px-4 py-10">

    <!-- 타이틀 -->
    <h2 class="text-2xl font-bold text-gray-800 mb-6 flex items-center gap-2">
        <i class="fa-solid fa-calendar-days text-blue-600"></i>
        내 시간표
    </h2>

    <!-- 시간표 전체 박스 -->
    <div class="bg-white shadow-md rounded-xl border border-gray-200 p-6">

        <!-- 시간표 테이블 -->
        <table class="w-full border-collapse text-sm text-gray-700">

            <!-- 요일 헤더 -->
            <thead>
            <tr class="bg-blue-50">
                <th class="w-20 py-3 border">교시</th>
                <th class="py-3 border">월</th>
                <th class="py-3 border">화</th>
                <th class="py-3 border">수</th>
                <th class="py-3 border">목</th>
                <th class="py-3 border">금</th>
            </tr>
            </thead>

            <!-- 본문 -->
            <tbody>

            {{#periods}}
            <tr>
                <!-- 교시 번호 -->
                <td class="text-center py-3 font-semibold border bg-gray-50">
                    {{period}}
                </td>

                {{#yoils}}
				<td class="h-20 border align-top">
				
				    {{#timetable}}
				    <div 
				        class="w-full h-full flex flex-col items-center justify-center 
				               bg-blue-100 text-blue-800 rounded cursor-pointer hover:bg-blue-200 transition"
				        onclick="location.href='/studentCourseDetail?courseNo={{courseNo}}'">
				        
				        <p class="font-semibold text-blue-900 text-xs">
				            {{courseName}}
				        </p>
				        <p class="text-[11px] text-gray-600">
				            {{courseClassroom}}
				        </p>
				        <p class="text-[11px] text-gray-500">
				            {{courseTimeStart}}~{{courseTimeEnd}}교시
				        </p>
				    </div>
				    {{/timetable}}
				
				    {{^timetable}}
				    <div class="w-full h-full"></div>
				    {{/timetable}}
				
				</td>
				{{/yoils}}
                

            </tr>
            {{/periods}}

            </tbody>
        </table>

    </div>

</main>

{{> studentLayout/studentFooter }}

</body>
</html>
