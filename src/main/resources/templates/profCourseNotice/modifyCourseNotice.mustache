<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>공지사항 수정</title>

    <link rel="stylesheet" as="style" crossorigin 
        href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.8/dist/web/static/pretendard.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" 
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        body { font-family: Pretendard, sans-serif; }
        input, textarea {
            border: 1px solid #D1D5DB;
            padding: 8px 10px;
            border-radius: 6px;
            width: 100%;
            box-sizing: border-box;
        }
    </style>
</head>

<body class="text-gray-800">

    {{> profLayout/profHeader }}

    <main class="max-w-4xl mx-auto px-4 py-10">
        
        <div class="bg-white rounded-xl shadow-lg p-8 border border-gray-100">

            <h2 class="text-2xl font-bold text-gray-800 mb-6 border-b pb-3">
                공지사항 수정
            </h2>

            <form action="/modifyCourseNotice" method="post" class="space-y-6">

                <input type="hidden" name="courseNoticeNo" value="{{courseNotice.courseNoticeNo}}">
                <input type="hidden" name="courseNo" value="{{courseNotice.courseNo}}">

                <table class="min-w-full divide-y divide-gray-200">
                    <tbody class="bg-white divide-y divide-gray-200">

                        <tr>
                            <th class="w-1/5 bg-gray-50 px-4 py-3 text-left text-sm font-medium">제목</th>
                            <td class="px-4 py-3">
                                <input type="text" name="courseNoticeTitle"
                                       value="{{courseNotice.courseNoticeTitle}}" required>
                            </td>
                        </tr>

                        <tr>
                            <th class="bg-gray-50 px-4 py-3 text-left text-sm font-medium">내용</th>
                            <td class="px-4 py-3">
                                <textarea name="courseNoticeContent" rows="10" required>{{courseNotice.courseNoticeContent}}</textarea>
                            </td>
                        </tr>

                    </tbody>
                </table>

                <div class="flex justify-end gap-4">
                    <button type="submit"
                        class="bg-blue-600 text-white px-6 py-2 rounded-lg text-sm font-medium hover:bg-blue-700 shadow-md">
                        <i class="fa-solid fa-check mr-1"></i> 수정 완료
                    </button>

                    <a href="/courseNoticeDetail?courseNoticeNo={{courseNotice.courseNoticeNo}}"
                        class="bg-gray-300 text-gray-700 px-6 py-2 rounded-lg text-sm font-medium hover:bg-gray-400">
                        취소
                    </a>
                </div>

            </form>

        </div>
    </main>

    {{> profLayout/profFooter }}

</body>
</html>
