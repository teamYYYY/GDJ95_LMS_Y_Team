<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>문의 수정하기</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/pretendard/dist/web/static/pretendard.css" />

    <style>
        body {
            font-family: 'Pretendard', sans-serif;
            background-color: #F5F7FA;
        }
    </style>
</head>

<body class="text-gray-800">

{{> studentLayout/studentHeader }}

<main class="max-w-3xl mx-auto px-5 py-10">

    <a href="/courseQuestionDetail?courseQuestionNo={{question.courseQuestionNo}}"
       class="text-sm text-gray-500 hover:text-gray-700">
        ← 돌아가기
    </a>

    <div class="bg-white rounded-2xl shadow-md border border-gray-100 p-8 mt-6">

        <h1 class="text-xl font-bold text-gray-900 flex items-center gap-2 mb-6">
            <i class="fa-solid fa-pen-to-square text-blue-600"></i>
            문의 수정
        </h1>

        <form action="/courseQuestionEdit" method="post" class="space-y-6">

            <input type="hidden" name="courseQuestionNo" value="{{question.courseQuestionNo}}">
            <input type="hidden" name="courseNo" value="{{question.courseNo}}">

            <!-- 제목 -->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">제목</label>
                <input type="text"
                       name="courseQuestionTitle"
                       value="{{question.courseQuestionTitle}}"
                       required
                       class="w-full rounded-lg border border-gray-300 text-sm p-3
                              focus:ring-blue-500 focus:border-blue-500" />
            </div>

            <!-- 내용 -->
            <div>
                <label class="block text-sm font-medium text-gray-700 mb-1">내용</label>
                <textarea name="courseQuestionContent" rows="6" required
                          class="w-full rounded-lg border border-gray-300 text-sm p-3
                                 focus:ring-blue-500 focus:border-blue-500">{{question.courseQuestionContent}}</textarea>
            </div>

            <!-- 비밀글 체크박스 -->
            <div class="flex items-center gap-2 text-sm">
                <input type="checkbox" name="privatePost"
                       class="w-4 h-4"
                       {{#question.privatePost}}checked{{/question.privatePost}}>
                <span class="text-gray-700">비밀글로 설정</span>
            </div>

            <!-- 버튼 -->
            <div class="flex justify-end gap-3 pt-4">
                <a href="/courseQuestionDetail?courseQuestionNo={{question.courseQuestionNo}}"
                   class="px-4 py-2 rounded-lg border border-gray-300 text-sm text-gray-600 hover:bg-gray-50">
                    취소
                </a>

                <button
                        class="px-5 py-2 rounded-lg bg-blue-600 text-white text-sm font-medium hover:bg-blue-700">
                    수정완료
                </button>
            </div>

        </form>

    </div>

</main>

</body>
</html>
