<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>학사관리시스템 로그인</title>
    <link rel="preconnect" href="https://cdn.jsdelivr.net">
    <link href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/static/pretendard.css" rel="stylesheet">
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        body, html { height:100%; font-family:'Pretendard', sans-serif; background:#0f172a; }

        .wrapper {
            min-height:100vh;
            display:flex;
            align-items:center;
            justify-content:center;
            padding:40px 20px;
            background:linear-gradient(135deg,#1e293b,#0f172a);
        }

        .card {
            width:1120px;
            max-width:95vw;
            background:white;
            border-radius:32px;
            overflow:hidden;
            box-shadow:0 32px 80px rgba(0,0,0,0.4);
            display:flex;
        }

        /* 왼쪽 사진 */
        .left {
            flex:1;
            position:relative;
            background:url('/images/school-building.jpg') center/cover no-repeat;
        }
        .left::before {
            content:''; position:absolute; inset:0;
            background:linear-gradient(to top,rgba(15,23,42,0.9),rgba(15,23,42,0.4) 50%,transparent);
        }
        .left-text {
            position:absolute; left:50px; bottom:50px; color:white;
            font-size:34px; font-weight:800; line-height:1.2;
        }
        .left-text span { font-size:18px; font-weight:500; opacity:0.95; }

        /* 오른쪽 폼 */
        .right {
            width:460px;
            padding:60px 55px 70px;   /* ← 아래쪽 패딩 크게 늘림 */
            display:flex;
            flex-direction:column;
            justify-content:flex-start;
        }

        .logo {
            width:90px; height:90px;
            background:#3b82f6;
            border-radius:50%;
            display:flex; align-items:center; justify-content:center;
            margin:0 auto 30px;
            box-shadow:0 12px 30px rgba(59,130,246,0.35);
        }

        .title { text-align:center; font-size:26px; font-weight:800; color:#1e293b; margin-bottom:8px; }
        .subtitle { text-align:center; font-size:15px; color:#64748b; margin-bottom:50px; }

        label { display:block; font-size:15px; font-weight:700; color:#374151; margin-bottom:10px; }
        input {
            width:100%; height:56px; padding:0 20px; border:1px solid #e2e8f0;
            border-radius:16px; background:#f8fafc; font-size:16px;
            transition:all .3s;
        }
        input:focus { outline:none; border-color:#3b82f6; background:white;
            box-shadow:0 0 0 4px rgba(59,130,246,0.15); }

        .btn {
            width:100%; height:58px; background:#3b82f6; color:white;
            border:none; border-radius:16px; font-size:18px; font-weight:700;
            cursor:pointer; margin-top:12px; transition:all .3s;
        }
        .btn:hover { background:#2563eb; transform:translateY(-1px); }

        .find { text-align:center; margin:32px 0 40px; }
        .find a { color:#64748b; font-size:14px; text-decoration:none; }
        .find a:hover { text-decoration:underline; }

        .test {
            background:#f1f5f9; padding:18px 20px; border-radius:14px;
            font-size:13.5px; color:#475569; text-align:center; line-height:1.7;
        }

        .footer {
            margin-top:30px; text-align:center; font-size:13px; color:#94a3b8; line-height:1.7;
        }

        .error { color:#ef4444; font-size:14px; text-align:center; margin-bottom:16px; font-weight:600; }
    </style>
</head>
<body>

<body>

<div class="wrapper">
    <div class="card">
        <!-- 왼쪽 -->
        <div class="left">
            <div class="left-text">
                학사관리시스템
                <span>Academic Management System</span>
            </div>
        </div>

        <!-- 오른쪽 -->
        <div class="right">
            <div class="logo">
                <svg width="54" height="54" viewBox="0 0 24 24" fill="white">
                    <path d="M12 2L1 9L12 16L23 9L12 2Z" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
                    <path d="M12 16V22" stroke="white" stroke-width="2" stroke-linecap="round"/>
                    <path d="M6 12L12 16L18 12" stroke="white" stroke-width="2" stroke-linecap="round"/>
                </svg>
            </div>

            <h1 class="title">학사관리시스템</h1>
            <p class="subtitle">로그인하여 시작하세요</p>

            {{#error}}
            <div class="error">{{error}}</div>
            {{/error}}

            <form action="/loginForm" method="post">
                <label for="userId">학번</label>
                <input type="text" id="userId" name="userId" placeholder="학번을 입력하세요"
                       required autofocus>

                <label for="userPassword" style="margin-top:24px;">비밀번호</label>
                <input type="password" id="userPassword" name="userPassword" placeholder="비밀번호를 입력하세요" required>

                <button type="submit" class="btn">로그인</button>
            </form>

            <div class="find"><a href="#">비밀번호 찾기</a></div>

            <div class="test">
                테스트 계정: admin (관리자) / 20240001 (학생)
            </div>

            <div class="footer">
                문의사항 안내: 02-000-0000<br>
                © 2025 학사관리시스템. All rights reserved.
            </div>
        </div>
    </div>
</div>

</body>

</body>
</html>