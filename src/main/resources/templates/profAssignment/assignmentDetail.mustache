<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>과제 상세보기</title>

    <link rel="stylesheet" as="style" crossorigin
        href="https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.8/dist/web/static/pretendard.css" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
        href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        body { font-family: Pretendard, sans-serif; }
    </style>
</head>

<body class="text-gray-800">

    {{> profLayout/profHeader }}

    <main class="max-w-7xl mx-auto px-4 py-8 space-y-6">

        <div class="bg-white rounded-xl shadow-sm p-8 border border-gray-100 space-y-6">

            <!-- 제목 -->
            <div class="pb-4 border-b border-gray-200">
                <h2 class="text-2xl font-bold text-gray-800">과제 상세보기</h2>
            </div>

            <!-- 상세 정보 박스 -->
            <div class="divide-y divide-gray-100 border border-gray-200 rounded-lg overflow-hidden">

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">과제번호</div>
                    <div class="flex-1 px-4 py-3 text-sm text-gray-800">{{assignment.assignmentNo}}</div>
                </div>

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">강의번호</div>
                    <div class="flex-1 px-4 py-3 text-sm text-gray-800">{{assignment.courseNo}}</div>
                </div>

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">제목</div>
                    <div class="flex-1 px-4 py-3 text-sm font-semibold text-gray-900">{{assignment.assignmentTitle}}</div>
                </div>

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">내용</div>
                    <div class="flex-1 px-4 py-3 text-sm text-gray-700 whitespace-pre-wrap">{{assignment.assignmentDescription}}</div>
                </div>

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">마감일</div>
                    <div class="flex-1 px-4 py-3 text-sm text-gray-800">{{assignment.assignmentDueDate}}</div>
                </div>

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">상태</div>
                    <div class="flex-1 px-4 py-3 text-sm text-gray-800">
                        {{#assignment.isOpened}}진행중{{/assignment.isOpened}}
                        {{#assignment.isClosed}}종료{{/assignment.isClosed}}
                    </div>
                </div>

                <div class="flex">
                    <div class="w-32 px-4 py-3 bg-gray-50 text-sm font-medium text-gray-500 border-r border-gray-200">등록일</div>
                    <div class="flex-1 px-4 py-3 text-sm text-gray-800">{{assignment.createdate}}</div>
                </div>

            </div>

            <!-- 버튼 -->
            <div class="flex justify-end space-x-3 pt-6 border-t border-gray-100">

                <a href="/modifyAssignment?assignmentNo={{assignment.assignmentNo}}"
                   class="px-4 py-2 bg-indigo-600 text-white rounded-lg text-sm font-medium hover:bg-indigo-700 transition shadow-md">
                    <i class="fa-solid fa-pen-to-square mr-1"></i> 수정
                </a>

                <a href="/removeAssignment?assignmentNo={{assignment.assignmentNo}}"
                   onclick="return confirm('정말 삭제하시겠습니까?');"
                   class="px-4 py-2 bg-red-600 text-white rounded-lg text-sm font-medium hover:bg-red-700 transition shadow-md">
                    <i class="fa-solid fa-trash-can mr-1"></i> 삭제
                </a>

                <a href="/assignmentListByProf?courseNo={{assignment.courseNo}}"
                   class="px-4 py-2 bg-gray-400 text-white rounded-lg text-sm font-medium hover:bg-gray-500 transition shadow-md">
                    <i class="fa-solid fa-list mr-1"></i> 목록으로
                </a>
            </div>

        </div>

        <!-- 학생 제출 목록 -->
        <div class="bg-white rounded-xl shadow-sm p-8 border border-gray-100 space-y-4">

            <div class="pb-3 border-b mb-4">
                <h3 class="text-xl font-semibold text-gray-800 flex items-center">
                    <i class="fa-solid fa-users mr-2"></i> 학생 제출 목록
                </h3>
            </div>

            <table class="w-full text-sm">
                <thead class="bg-gray-50">
                    <tr class="text-gray-600">
                        <th class="py-2 px-3 text-left">학생명</th>
                        <th class="py-2 px-3 text-left">아이디</th>
                        <th class="py-2 px-3 text-left">파일</th>
                        <th class="py-2 px-3 text-left">내용</th>
                        <th class="py-2 px-3 text-center">제출 여부</th>
                        <th class="py-2 px-3 text-center">제출일</th>
                    </tr>
                </thead>

                <tbody>
				    {{#submissionList}}
				    <tr class="border-b">
				
				        <td class="py-2 px-3">{{writerName}}</td>
				        <td class="py-2 px-3">{{writerId}}</td>
				
				        <!-- 파일 -->
				        <td class="py-2 px-3">
				            {{#isSubmitted}}
				                {{#assignmentSubmissionFileUrl}}
				                <a href="{{assignmentSubmissionFileUrl}}" target="_blank" class="text-blue-600 hover:underline">
				                    파일보기
				                </a>
				                {{/assignmentSubmissionFileUrl}}
				                {{^assignmentSubmissionFileUrl}}-{{/assignmentSubmissionFileUrl}}
				            {{/isSubmitted}}
				            {{#isNotSubmitted}}-{{/isNotSubmitted}}
				        </td>
				
				        <!-- 내용 -->
				        <td class="py-2 px-3 truncate max-w-[200px]">
				            {{#isSubmitted}}{{assignmentSubmissionContent}}{{/isSubmitted}}
				            {{#isNotSubmitted}}-{{/isNotSubmitted}}
				        </td>
				
				        <!-- 제출 여부 -->
				        <td class="py-2 px-3 text-center">
				            {{#isSubmitted}}<span class="text-green-600 font-semibold">제출</span>{{/isSubmitted}}
				            {{#isNotSubmitted}}<span class="text-red-600 font-semibold">미제출</span>{{/isNotSubmitted}}
				        </td>
				        
				        <!-- 제출일 -->
				        <td class="py-2 px-3 text-center">
				            {{#isSubmitted}}{{createdate}}{{/isSubmitted}}
				            {{#isNotSubmitted}}-{{/isNotSubmitted}}
				        </td>
				
				    </tr>
				    {{/submissionList}}
				
				    {{^submissionList}}
				    <tr>
				        <td colspan="8" class="py-4 text-center text-gray-400">수강 중인 학생이 없습니다.</td>
				    </tr>
				    {{/submissionList}}
				</tbody>

            </table>

        </div>

    </main>

    {{> profLayout/profFooter }}

</body>
</html>
