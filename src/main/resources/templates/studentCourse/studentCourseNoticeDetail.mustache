<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <title>공지 상세보기</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/pretendard/dist/web/static/pretendard.css" />
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />

    <style>
        body { font-family: 'Pretendard', sans-serif; }
    </style>
</head>

<body class="bg-[#F5F7FA] text-gray-800">

{{> studentLayout/studentHeader }}

{{> studentCourse/_studentCourseSubNav }}

<!-- 컨테이너 -->
<div class="max-w-5xl mx-auto mt-10 bg-white shadow rounded-2xl p-8">

    <!-- 제목 영역 -->
    <div class="flex items-center gap-2 mb-5">
        <i class="fa-solid fa-bullhorn text-blue-600 text-xl"></i>
        <h2 class="text-2xl font-bold text-gray-800">
            {{detail.courseNoticeTitle}}
        </h2>
    </div>

    <!-- 상세 정보 -->
    <div class="text-sm text-gray-500 border-t border-gray-200 pt-4 pb-4 flex flex-wrap gap-6">
        <span><b>작성자:</b> {{detail.writerUserName}}</span>
        <span><b>조회수:</b> {{detail.courseNoticeViewCount}}</span>
        <span><b>등록일:</b> {{detail.createdate}}</span>
    </div>

    <!-- 내용 -->
    <div class="mt-6 text-gray-700 leading-relaxed whitespace-pre-line">
        {{detail.courseNoticeContent}}
    </div>

    <!-- 버튼 영역 -->
    <div class="mt-10 flex justify-end">
        <a href="/studentCourseNoticeList?courseNo={{courseNo}}"
           class="px-5 py-2 text-sm bg-gray-100 border border-gray-300 rounded-lg hover:bg-gray-200">
        <i class="fa-solid fa-arrow-left text-gray-500"></i>    
            목록으로
        </a>
    </div>
</div>

</body>
</html>
