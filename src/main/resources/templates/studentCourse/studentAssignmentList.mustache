<!-- 학생 과제 목록 -->
<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title>과제 목록 | 학사관리시스템</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/pretendard/dist/web/static/pretendard.css" />
    <link rel="stylesheet"
          href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
    <style>
        body { font-family: 'Pretendard', sans-serif; }
    </style>
</head>
<body class="bg-[#F5F7FA] text-gray-800">

{{> studentLayout/studentHeader }}

<main class="max-w-6xl mx-auto px-4 py-10">

    <!-- 상단 타이틀 카드 -->
    <section class="mb-8">
        <div class="bg-white rounded-2xl shadow-md border border-gray-100 px-6 py-5 flex items-center justify-between">
            <div>
                <p class="text-xs font-semibold text-blue-600 uppercase tracking-wide mb-1">
                    <i class="fa-solid fa-clipboard-list mr-1"></i> 과제
                </p>
                <h1 class="text-2xl font-bold text-gray-900">과제 목록</h1>
                <p class="mt-1 text-xs text-gray-500">
                    현재 수강 중인 강의의 과제를 확인하고 제출 상태를 확인할 수 있습니다.
                </p>
            </div>
        </div>
    </section>
{{> studentCourse/_studentCourseSubNav }}
    <!-- 과제 리스트 -->
    <section class="bg-white rounded-2xl shadow-md border border-gray-100 px-4 py-5">
        {{#assignmentList}}
        <ul class="divide-y divide-gray-100">
            {{#assignmentList}}
            <li class="py-4 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">

                <!-- 제목 + 링크 -->
                <div class="min-w-0">
                    <a href="/student/assignment/detail?assignmentNo={{assignmentNo}}&courseNo={{courseNo}}"
                       class="text-sm sm:text-base font-medium text-gray-900 hover:text-blue-600 line-clamp-2">
                        {{assignmentTitle}}
                    </a>
                    <p class="mt-1 text-xs text-gray-500">
                        마감일: {{assignmentDeadline}}
                    </p>
                </div>

                <!-- 상태 뱃지 -->
                <div class="flex items-center gap-3 sm:ml-4">

                    {{#assignmentList}}
                    <span class="inline-flex items-center px-3 py-1 rounded-full bg-emerald-50 text-emerald-700 text-xs font-semibold">
                        <i class="fa-solid fa-check mr-1"></i> 제출 완료
                    </span>
                    {{/assignmentList}}

                    {{^assignmentList}}
                    <span class="inline-flex items-center px-3 py-1 rounded-full bg-red-50 text-red-600 text-xs font-semibold">
                        <i class="fa-solid fa-circle-exclamation mr-1"></i> 미제출
                    </span>
                    {{/assignmentList}}
                </div>

            </li>
            {{/assignmentList}}
        </ul>
        {{/assignmentList}}

        {{^assignmentList}}
        <div class="py-10 text-center text-sm text-gray-400">
            <i class="fa-regular fa-clipboard mb-2 block text-xl"></i>
            등록된 과제가 없습니다.
        </div>
        {{/assignmentList}}
    </section>

</main>

{{> studentLayout/studentFooter }}

</body>
</html>
